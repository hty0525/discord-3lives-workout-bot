# 변경사항

## v3.0.0

- 운동 인증을 `/운동 횟수:` 슬래시 명령으로 변경. 봇이 운동 인증 채널에 남기는 기록 메시지만 집계
- 채팅의 `x/y` 자유 텍스트 인식 제거 — 잡담 속 `운동 2/3` 같은 문장이 인증으로 잡히던 문제 수정
- 같은 주에 여러 번 올리면 최댓값 대신 가장 나중 기록을 사용 — 본인이 `/운동`을 다시 올려 정정 가능
- 자유 텍스트 인식에 딸려 있던 ✅ 자동 리액션 제거
- `/목표조절`을 일반 사용자도 사용 가능 — 본인 목표(1~5회)만 변경, 다른 사람은 관리자만

## v2.1.0

- `2/3` → 1회 부족 → 목숨 1 차감
- `1/3` → 2회 부족 → 목숨 2 차감
- `0/3` → 3회 부족 → 목숨 3 차감
- 차감 도중 `목0`이 되면 `💀` 표시 후 즉시 `목3`으로 초기화
- 초기화 뒤에도 남은 차감 횟수가 있으면 계속 차감
